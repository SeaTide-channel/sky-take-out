meta {
  name: set dish soup status 0
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/admin/dish/status/0?id={{dishSoupId}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.code: eq 1
  res.body.msg: isNull
  res.body.data: isNull
}
