meta {
  name: set dish soup status 1
  type: http
  seq: 12
}

post {
  url: {{baseUrl}}/admin/dish/status/1?id={{dishSoupId}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.code: eq 1
  res.body.msg: isNull
  res.body.data: isNull
}
