meta {
  name: get dish list no params
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/admin/dish/list
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.code: eq 1
  res.body.msg: isNull
  res.body.data: isNull
}
