meta {
  name: set setmeal status 0
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/admin/setmeal/status/0?id={{setmealId}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.code: eq 1
  res.body.msg: isNull
  res.body.data: isNull
}
