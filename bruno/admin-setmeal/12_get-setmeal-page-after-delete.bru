meta {
  name: get setmeal page after delete
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/admin/setmeal/page?page=1&pageSize=10
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.code: eq 1
  res.body.data.total: eq 0
  res.body.data.records: length 0
}
