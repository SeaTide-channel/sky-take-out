meta {
  name: get setmeal by id after status 0
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/admin/setmeal/{{setmealId}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.code: eq 1
  res.body.data.id: isDefined
  res.body.data.status: eq 0
}
